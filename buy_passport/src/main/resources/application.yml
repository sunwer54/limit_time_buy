#当前服务端口
server:
  port: 8084
spring:
  #关联redis模块配置文件
  profiles:
    active: redis
  #视图解析器
  mvc:
    view:
      prefix: /pages/
      suffix: .html
  #当前服务器名称
  application:
    name: passport-service
  cloud:
    #nacos配置中心
    nacos:
      discovery:
        server-addr: localhost:8848
